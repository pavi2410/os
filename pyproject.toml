[project]
name = "os-integration-tests"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
build = [
    "pyfatfs>=1.1.0",
]
dev = [
    "pexpect>=4.9.0",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["test/integration"]
addopts = "-ra"
